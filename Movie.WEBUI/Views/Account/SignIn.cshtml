@model SignInViewModel
@{
    ViewData["Title"] = "SignIn";
}
@*
<div class="modal-overlay">
    <div class="modal-content">
        <span class="close-button">&times;</span>
        <h2 class="modal-title">Sign In</h2>
        <form class="formModal" asp-action="SignIn">
            <label asp-for="UserName" for="username" class="labelModal">Username</label>
            <input asp-for="UserName" type="text" id="username" name="username" required>

            <label asp-for="Password" for="password" class="labelModal">Password</label>
            <input asp-for="Password" type="password" id="password" name="password" required>

            <div class="form-group">
                <input asp-for="RememberMe" type="checkbox" id="remember-me" name="remember-me">
                <label asp-for="RememberMe" for="remember-me" class="remember-me mx-2">Remember me</label>
                <a href="#" class="forgot-password">Forgot password?</a>
            </div>
            <button type="submit" class="btn-signin">Sign In</button>
        </form>
        <p>Don't have an account? <a href="#" class="sign-up">Sign Up</a></p>
        <div class="social-icons">
            <a href="#"><i class="fab fa-facebook"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
        </div>
    </div>
</div>
*@
