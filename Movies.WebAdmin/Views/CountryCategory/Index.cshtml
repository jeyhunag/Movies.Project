@model List<CountryCategoryDto>
@{
    ViewData["Title"] = "Index";
}

<div class="row" style="display:flex; align-items:center; justify-content:center;">
    <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12" style="margin-top:75px;">
        <div class="sparkline8-list">
            <div class="sparkline8-hd">
                <div class="main-sparkline8-hd text-white">
                    <h1 style="color:white; display:flex; align-items:center; justify-content:center;">Country Category</h1>
                    <div style="float:right">
                        <a asp-action="Create" class="btn btn-custon-four btn-primary" >Creat Country</a>
                    </div>
                </div>
            </div>
            <div class="sparkline8-graph">
                <div class="static-table-list">
                    <table class="table ">
                        <thead>
                            <tr style="color:white; font-size:19px;">
                                <th>Id</th>
                                <th> Country</th>
                                <th style="display:flex; text-align:end; justify-content:end;">Update || Delete </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr style="color:white;">
                                    <td>@item.Id</td>
                                    <td>@item.Name</td>
                                    <td style="display:flex; text-align:end; justify-content:end;">
                                        <a class="btn btn-custon-four btn-warning" asp-action="Update" asp-route-id="@item.Id" style="margin-right:8px;">
                                            <i class="fa-solid fa-pen-to-square"></i>
                                            Update
                                        </a>
                                        <a class="btn btn-custon-four btn-danger" asp-action="Delete" asp-route-id="@item.Id">
                                            <i class="fa-solid fa-trash-can"></i>
                                            Delete
                                        </a>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>